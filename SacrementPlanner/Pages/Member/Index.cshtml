@page
@model SacrementPlanner.Pages.Member.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Members[0].MembersName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Members) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MembersName)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.MembersId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.MembersId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.MembersId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
